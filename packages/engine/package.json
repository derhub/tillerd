{
  "name": "@athing/engine",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "mkdir -p \"${ATHING_DIR}\" && bun build --minify --target bun src/ingress/notify.ts --outfile \"${ATHING_DIR}/notify.mjs\"",
    "check-types": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@athing/daemon": "workspace:*",
    "@athing/sdk": "workspace:*",
    "valibot": "^1.4.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
