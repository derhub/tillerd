{
  "name": "@tillerd/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "cargo clippy --all-targets --locked -- -D warnings",
    "fmt": "cargo fmt",
    "fmt:check": "cargo fmt --check",
    "test": "cargo test"
  }
}
