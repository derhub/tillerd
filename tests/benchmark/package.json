{
  "name": "@tillerd/benchmark",
  "private": true,
  "type": "module",
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json",
    "bench": "bun index.ts",
    "check-deps": "bun outdated"
  },
  "dependencies": {
    "@tillerd/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
