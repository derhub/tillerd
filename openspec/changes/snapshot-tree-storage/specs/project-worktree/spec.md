## REMOVED Requirements

### Requirement: Worktree row lifecycle

**Reason**: Worktree entity dropped in 0.0.15 (ADR-0033); a working directory is just `cwd`.

**Migration**: None — no replacement; surfaces carry `cwd` relative to the project root.

### Requirement: Worktree step — create, cd, run

**Reason**: Worktree entity dropped in 0.0.15 (ADR-0033); a working directory is just `cwd`.

**Migration**: None — no replacement; surfaces carry `cwd` relative to the project root.

### Requirement: Worktree ownership by project

**Reason**: Worktree entity dropped in 0.0.15 (ADR-0033); a working directory is just `cwd`.

**Migration**: None — no replacement; surfaces carry `cwd` relative to the project root.
