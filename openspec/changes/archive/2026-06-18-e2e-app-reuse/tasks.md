# Tasks

- [x] 1. Stand up the shared-app harness and prove it with the boot spec: `shared-app.ts` (`launchSharedApp` moving `remote()` + wait-`services: ready` out of `launchReadyApp`, `getApp`, `teardownSharedApp`), `setup.ts` preload (`beforeAll(launchSharedApp)` / `afterAll(teardownSharedApp)` / `beforeEach(() => resetToHome(getApp()))`), `resetToHome` + `uniqueName` in `helpers.ts` (keep `launchReadyApp` for resume); wire `run.sh`'s three invocations (main dev `--preload setup.ts <*.test.ts except resume>`, `resume.test.ts` standalone, bundled `--preload setup.ts boot.test.ts`); convert `boot.test.ts` to assert `getApp()` is ready. A green boot run proves the preload launches exactly once.
- [x] 2. Migrate every scenario spec off `launchReadyApp()` onto `getApp()` — drop each spec's `afterEach`/launch, switch `Project ${Date.now()}` to `uniqueName`, and reorder each multi-test spec (`command-center`, `logs-filter`, `panel-detach`, `workspace-management`) once to confirm order-independence.
- [x] 3. Verify gate: `bash tests/desktop-e2e/run.sh` green with `E2E_BUNDLED` (dev scenario suite + resume + bundled boot), run the dev suite a second time to confirm no cross-run leakage, and `pgrep -f tillerd-desktop` shows no orphan after. Fix any leakage or fallout before marking done.
