## 1. Placement geometry

- [x] 1.1 TDD the versioned panel-tree model: normalized per-group sizes, nested isolation, strict validation, and clean-cutover deserialization.
- [x] 1.2 TDD controlled split rendering, completed resize persistence, equal reset, and nested desktop reload behavior.

## 2. Verification

- [x] 2.1 Run `/opsx:verify add-placement-geometry` and fix every finding; run `bun run format`, `bun run verify`, and `bun run e2e`, fixing every failure; complete correctness, security, performance, and simplification review and fix every blocking finding.
