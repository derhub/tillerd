{
  "name": "@tillerd/memorya",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "lint": "cargo clippy --all-targets --locked -- -D warnings",
    "fmt": "cargo fmt",
    "fmt:check": "cargo fmt --check",
    "test": "cargo test",
    "eval": "cargo run --bin memorya-eval"
  },
  "dependencies": {
    "@tillerd/contracts": "workspace:*",
    "@tillerd/gate-client": "workspace:*",
    "@tillerd/paths": "workspace:*",
    "@tillerd/redact": "workspace:*"
  }
}
