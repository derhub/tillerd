{
  "name": "@athing/gate",
  "version": "0.1.0",
  "bin": {
    "athing-gate": "../../target/release/athing-gate"
  },
  "type": "module",
  "scripts": {
    "dev": "cargo build --release && ../../target/release/athing-gate",
    "build": "cargo build --release",
    "lint": "cargo clippy --all-targets --locked -- -D warnings",
    "fmt": "cargo fmt",
    "fmt:check": "cargo fmt --check",
    "test": "cargo test",
    "check-deps": "cargo outdated"
  }
}
