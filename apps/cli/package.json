{
  "name": "@tillerd/cli",
  "version": "0.0.0",
  "bin": {
    "tillerd": "src/index.ts"
  },
  "type": "module",
  "scripts": {
    "check-types": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "bun test",
    "check-deps": "bun outdated"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
