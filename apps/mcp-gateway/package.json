{
  "name": "@tillerd/mcp-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Rust MCP gateway daemon. Built with cargo, outside the Bun/turbo graph.",
  "scripts": {
    "build": "cargo build --release",
    "lint": "cargo clippy --all-targets --locked -- -D warnings",
    "fmt": "cargo fmt",
    "fmt:check": "cargo fmt --check",
    "test": "cargo test",
    "check-deps": "cargo outdated"
  }
}
