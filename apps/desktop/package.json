{
  "name": "a-thing-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --ci",
    "tauri": "tauri",
    "test": "cd ./src-tauri && cargo test",
    "check-deps": "cd ./src-tauri && cargo outdated"
  },
  "dependencies": {
    "ui": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
